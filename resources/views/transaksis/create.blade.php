@extends('layouts.app')
@section('content')<h1>Buat Transaksi</h1><form class="form" method="post" action="{{ route('transaksis.store') }}">@csrf<label>Meja<select name="meja_id" required>@foreach($mejas as $meja)<option value="{{ $meja->id }}">Meja {{ $meja->nomor_meja }}</option>@endforeach</select></label><label>Karyawan<select name="karyawan_id" required>@foreach($karyawans as $karyawan)<option value="{{ $karyawan->id }}">{{ $karyawan->nama_karyawan }}</option>@endforeach</select></label><label>Tipe pesanan<select name="tipe_pesanan"><option>dine-in</option><option>take-away</option></select></label><label>Produk<select name="items[0][produk_id]" required>@foreach($produks as $produk)<option value="{{ $produk->id }}">{{ $produk->nama_produk }} - Rp {{ number_format($produk->harga_jual, 0, ',', '.') }}</option>@endforeach</select></label><label>Jumlah<input type="number" name="items[0][qty]" value="1" min="1" required></label><button>Simpan transaksi</button></form>@endsection
