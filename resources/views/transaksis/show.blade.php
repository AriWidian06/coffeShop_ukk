@extends('layouts.app')
@section('content')<h1>Transaksi #{{ $transaksi->id }}</h1><p>Status: <strong>{{ $transaksi->status_pesanan }}</strong></p><p>Total: Rp {{ number_format($transaksi->total_harga, 0, ',', '.') }}</p><table><tr><th>Produk</th><th>Qty</th><th>Subtotal</th></tr>@foreach($transaksi->detail_transaksis as $detail)<tr><td>{{ $detail->produk->nama_produk }}</td><td>{{ $detail->QTY }}</td><td>Rp {{ number_format($detail->subtotal, 0, ',', '.') }}</td></tr>@endforeach</table><form class="form" method="post" action="{{ route('transaksis.status', $transaksi) }}">@csrf @method('PATCH')<label>Ubah status<select name="status_pesanan">@foreach(['pending','ready','prosesed','completed'] as $status)<option @selected($transaksi->status_pesanan === $status)>{{ $status }}</option>@endforeach</select></label><button>Perbarui status</button></form>@if(!$transaksi->pembayaran)<form class="form" method="post" action="{{ route('pembayarans.store') }}">@csrf<input type="hidden" name="transaksi_id" value="{{ $transaksi->id }}"><label>Jumlah bayar<input type="number" step="0.01" name="jumlah_bayar" min="{{ $transaksi->total_harga }}" required></label><label>Metode<select name="metode_pembayaran"><option>cash</option><option>QRIS</option></select></label><button>Bayar</button></form>@endif<a href="{{ route('transaksis.receipt', $transaksi) }}">Cetak struk</a>@endsection
